{% extends 'base.html.twig' %}

{% block title %}Validez ma commande! -YK.DESIGN{% endblock %}

{% block content %}
<h2>ma commande</h2>
<p>Vous pouvez choisir vos preferences avant de commander sur le site</p>
<hr>
<div class="row"> 
<div class="col-md-6">
{# <b>choisir mon addresse de livraison</b> <br> #}
{% set formHtml %}
{{ form_start(form, {action:path('order_recap')}) }}
{{ form_label(form.addresses, 'Choisissez votre addresse de livraisons') }}
<a href ="{{ ('ddress_add') }}">Ajoutez une nouvelle addresse de livraison</a>
{{ form_end(form) }}

{% endset %}
{{ formHtml| replace ({'[br]' : '<br>'})|raw }}
</div>
<div class="col-md-6">
<div class ="text-center">
<p>le recapitulatif de vos produits </p>
</div>
<div class ="order-summary">
{% for product in cart %}
<div class="row">
<div class="col-2 my-auto">
<img src="/images/{{ product.product.illustration }}" alt="{{ product.product.name }}" height="75px">
</div>
<div class = "col-8">
      {{ product.product.name }} <br> <small>{{ product.product.subtitle }}</small>
</div>
<div class="col-2 my-auto">
x {{ product.quantity }} </div>
</div>
</div>
{% endfor %}
</div>
</div>
</div>
{% endblock %}
